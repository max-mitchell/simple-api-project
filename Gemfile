# Gemfile
source 'https://rubygems.org'

gem 'sinatra'
gem 'activerecord'
gem 'standalone_migrations'
gem 'pg'

# For testing
group :test do
    gem 'minitest'
    gem 'minitest-around'
    gem 'rack-test'
    gem 'database_cleaner-active_record'
end
