# Gemfile
source 'https://rubygems.org'

ruby '2.7.0'

gem 'sinatra'
gem 'activerecord'
gem 'standalone_migrations'
gem 'pg'
gem 'puma'

# For testing
group :test do
    gem 'minitest'
    gem 'minitest-around'
    gem 'rack-test'
    gem 'database_cleaner-active_record'
end
